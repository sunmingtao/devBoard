# Legacy GitHub Actions workflows

These workflows are kept for reference only.

They were moved out of `.github/workflows` so GitHub does not run them while the
new CI/CD pipeline is designed from scratch.

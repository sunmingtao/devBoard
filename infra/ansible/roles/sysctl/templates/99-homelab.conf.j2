# Managed by Ansible.
{% if homelab_sysctl_settings | length == 0 %}
# No homelab sysctl settings are configured.
{% else %}
{% for name, value in homelab_sysctl_settings | dictsort %}
{{ name }} = {{ value }}
{% endfor %}
{% endif %}

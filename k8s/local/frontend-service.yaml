apiVersion: v1
kind: Service
metadata:
  name: devboard-frontend
  namespace: devboard
spec:
  selector:
    app: devboard-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort